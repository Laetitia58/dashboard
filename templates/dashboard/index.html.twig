{% extends 'base.html.twig' %}

{% block title %}Hello DashboardController!{% endblock %}

{% block body %}

<section class="articles">

    {% for article in articles %}
        <article>
            <h2>{{ article.Nom }}</h2>
            <div class"metadata">Achat le {{ article.DateAchat | date ('d/m/Y') }}</div>
                <div class"metadata">Fin de garantie le {{ article.DateGarantie | date ('d/m/Y') }}</div>
            <div class"content">
                <img src="{{ article.PhotoTicket }}" width="350rem" height="150rem" alt="">
                <div class"metadata"><h5>Prix en €:  {{ article.Prix }}</h5></div>
                <a href={{ path('dashboard_show', {'id':article.id}) }} class="btn.btn-primary">Zoom</a>
            </div>    
        </article>
    {% endfor %}

</section>

{% endblock %}
